# Replace these with your actual Stripe keys
# Get them from https://dashboard.stripe.com/apikeys

# Publishable key (safe to use in frontend)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Secret key (NEVER put this in frontend - only for backend)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Supabase Configuration for Cross-Device Data Sync
# To enable cross-device synchronization, you need to:
# 1. Create a free Supabase account at https://supabase.com
# 2. Create a new project
# 3. Copy your project URL and anon key below
# 4. Copy this file to .env

# Your Supabase project URL (found in Settings > API)
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon public key (found in Settings > API)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Note: The app will work without these values using localStorage only,
# but products won't sync across devices until Supabase is configured.